-- List all records with score >= 10 in the second_table of hbtn_0c_0
-- display score and name and order by score DESC
SELECT `score`, `name` FROM `second_table` WHERE `score` >= 10
ORDER BY score DESC;
